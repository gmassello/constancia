services:
  db:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_USER: constancia
      POSTGRES_PASSWORD: constancia
      POSTGRES_DB: constancia
    ports:
      - "5432:5432"
    volumes:
      - constancia-db:/var/lib/postgresql/data

volumes:
  constancia-db:
